# ── Django ──────────────────────────────────────────────────
SECRET_KEY=votre-cle-secrete-tres-longue-et-aleatoire
DEBUG=False
ALLOWED_HOSTS=votre-app.onrender.com,localhost

# ── Base de données (SQLite par défaut, PostgreSQL en production) ──
# DATABASE_URL=postgresql://user:password@host:5432/agroconnect

# ── CORS (URL du frontend) ───────────────────────────────────
FRONTEND_URL=https://agroconnect.vercel.app

# ── Cloudinary (stockage images) ─────────────────────────────
CLOUDINARY_CLOUD_NAME=votre_cloud_name
CLOUDINARY_API_KEY=votre_api_key
CLOUDINARY_API_SECRET=votre_api_secret

# ── Redis (WebSocket en production — optionnel) ───────────────
# REDIS_URL=redis://localhost:6379
