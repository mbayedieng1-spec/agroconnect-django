from django.apps import AppConfig


class ApiConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'api'

    def ready(self):
        # Start the auto-poster scheduler when Django starts
        from . import scheduler
        scheduler.start()
